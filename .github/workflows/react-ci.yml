name: CI

on:
push:
branches: ["main"]        # ✅ fixed
pull_request:
branches: ["main"]        # ✅ fixed

jobs:
build:
runs-on: ubuntu-latest    # ✅ fixed

```
step:                     # ❌ still wrong (should be 'steps')
  - name: Checkout repository
    uses: actions/checkout@v4   # ✅ fixed

  - name: Setup Node.js
    uses: actions/setup-node@v4
    with:
      node-version: "18"        # ⚠️ works but better without quotes

  - name: Install dependencies
    run: npm install ci         # ❌ still wrong

  - name: Type check (TS)
    run: npm build              # ❌ still wrong

  - name: Build project
    run: npm run start          # ❌ still wrong
```
